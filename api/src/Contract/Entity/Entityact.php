<?php

namespace App\Contract\Entity;

interface Entityact
{
    /**
     * The only use of this interface is to type with it instead of the object
     * type when we want to pass in parameter or return an entity.
     *
     * All classes that are entities must implement it.
     */
}
